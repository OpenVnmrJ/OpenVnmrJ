To add a tag to a repo, go to the repo from OpenVnmrJ
that has been cloned with ssh.

Create a tag with, for example,

git tag -a OpenVnmrJ_3.2A -m "OpenVnmrJ 3.2A"

Push that tag to the repo with, for example

git push origin OpenVnmrJ_3.2A
