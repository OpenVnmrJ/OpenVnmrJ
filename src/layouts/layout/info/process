Use the process panel to process and display data.

The Process button processes the data.

The Cancel button cancels the current command.  This is 
usually only needed for commands that take a long time.
