"macro Dghmqcidosy_setup"
axis='pd' pmode='full' gaussian
phase=1,2 f1coef='1 0 0 1 0 1 1 0'
setdecpars dm='nny'
dm='nny'
strstr(satmode,'y'):$e
if $e then 
  if satfrq<>tof then 
    write('error','satfrq not active, saturation must happen at the tof position!')
  endif
endif
