****************************************************************************
addnucleus 	add a new nucleus to existing probe file(M)
****************************************************************************
Syntax:		addnucleus(nucleus)

Description: 	Entries for nuclei not in the default probe file are 
		appended to the end of the file.  The argument should
		correspond to a nucleus in the nuctable.

Related:	addprobe

Example:	addnucleus('Si29')
