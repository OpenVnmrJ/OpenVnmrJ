
psgset	   Set up parameters for various pulse sequences
           using information in user or system parlib
           This macro, in general, will never need to be
           entered from the keyboard

Format:	  psgset(filename,parameter1,parameter2,...,parameterN)

          The parameters "seqfil" and "pslabel" are set the supplied
          file name.  Rather than returning the entire parameter file,
          psgset returns from one to eleven parameters from a parlib
          file.

Example:  psgset('cosy','dg','ap','ss','d1','axis','phase')

