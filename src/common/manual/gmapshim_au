gmapshim_au:	Start acquisition with gradient shimming (M)

	
        Description:    If wshim <> 'n' this macro checks the probe
        		file for lk gradient map and if one exist
        		starts gradient shimming following by acquisition
        		by invoking gmapshim('glideau').
        		If lk gradient mapname does not exist in the
        		probe file, then starts acquisition by invoking
        		au('wait').
        		
        Related:        gmapshim
        

