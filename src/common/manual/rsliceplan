rsliceplan - rsliceplan is an helper macro to iplan image planning.
     It combines the iplan data with sequence parameters to generate
     the absolute magnet frame data.  Other uses should use sliceplan.
