
*******************************************************************************
mv	 - 	rename (move) a file or directory
rename	 - 	rename (move) a file or directory
*******************************************************************************

  This command renames (moves) a file or directory.  It is the
  Unix mv command.

  Usage --  mv('fromfile','tofile')
  Usage --  rename('fromfile','tofile')

  Examples:

       mv('fromfile','tofile')
