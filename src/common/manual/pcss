/*************************************
pcss calculate & display the
     proton chemical shift spectrum
*************************************/


pcss		Calculate and display the Proton Chemical Shifts Spectrum (M)


   Syntax: 	pcss<(threshold, maxcc, maxw)>

   Description:	pcss is used to calculate and display the proton chemical 
   		shifts spectrum. The list of the chemical shifts
   		are saved in the file 'pcss.outpar'.
   		
   Argument:	'threshold' is used to determine whether a point is belong 
   		to a peak or is noise.
   		
   		'maxcc' is the maximum allowable coupling constant in the spectrum.
		
		'maxw' is the maximum width of a spin multiplet in the spectrum.

		If no parameter is used, the program will calculate the 
		threshold automatically and use default values for the 
		maximum  allowable coupling constant (20Hz) and the 
		maximum width of a spin multiplet (60Hz)
  
   Examples:	pcss
		pcss(10)
		pcss(9, 20, 80)
   		
   Related: 	do_pcss, nopcss

