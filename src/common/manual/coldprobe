
coldprobe  -	Tells system a coldprobe is present
warmprobe  -	Tells system a warm probe is present

If a C13 observe coldprobe is being used, the value of rof2 should
not be less than 350 usec.

The coldprobe macro tells the system that a coldprobe is present
so that the rof2 rule is enforced.

The warmprobe macro tells the system that a warm probe is present
so that the rof2 rule is not enforced,
