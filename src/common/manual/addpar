
The addpar command adds selected parameters to the current experiment.
You tell it what parameters you want in the command, as shown below:

        addpar( '2d' )         Add parameters for a 2D experiment
        addpar( '3d' )         Add parameters for a 3D experiment
        addpar( '4d' )         Add parameters for a 4D experiment
        addpar( '3rf' )        Add parameters for the 3rd RF channel
        addpar( 'fid' )        Add parameters for FID display
        addpar( 'image' )      Add parameters for Imaging
        addpar( 'll2d' )       Add parameters for 2D line listing
        addpar( 'downsamp' )   Add parameters for downsampling data
        addpar( 'oversamp' )   Add parameters for oversampling data
        addpar( 'ss' )         Add parameters for solvent suppression
        addpar( 'lp' )         Add parameters for Linear Prediction

When adding Linear Prediction parameters, you can specify the dimension
in the command:

        addpar( 'lp', 0 )      Add Linear Prediction Parameters
                               for the acquisition dimension
        addpar( 'lp', 1 )      Add Linear Prediction Parameters
                               for the 1st implicit dimension
        addpar( 'lp', 2 )      Add Linear Prediction Parameters
                               for the 2nd implicit dimension
