setglideexp:     Setup glide experiment from the command line (M)

	Syntex:		setglideexp('experiment')
	Description:	Sets up glide experiment from the command line
			or from tcl-dg.   The acquire def file is read
			from glide/exp/experiment directory and will do
			a dialog.
        
	Example:	setglideexp('AuH')        		
