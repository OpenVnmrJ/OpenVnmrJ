centerprobe: calculates probe position relative to the ISO-Center.

centerprobe will display the results on the graphics screen.
centerprobe('plot') will plot the results on the selected plotter.

Applicability:  Systems with gradient shimming installed.

Description:  A macro that calculates the rf probe position relative
to the ISO-center.  Using the Z2 plot from the current gradient map
this macro calculates  how far out of center the probe is.
