length	  Return length of input string

Format:	  length(string):$real

	  length returns the length in characters of an input string as
	  a single real number.

Examples: length('abc'):r1
	  length(solvent):$len
