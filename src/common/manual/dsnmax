**********************************
* dsnmax--calculates maximum S/N *
* dsnf1max--calculates maximum S/N in f1 dimension *
**********************************
Usage: dsnmax(<noise region>)
 	 If no argument, dsnmax finds the best S/N based on the noise
	 region defined by delta. If given the noise region as an
	 argument, e.g. dsnmax(400), the macro calculates the best
	 S/N ratio based on 400 Hz of noise region.

Usage: dsnf1max(<noise region>)
 	 If no argument, dsnf1max finds the best S/N based on the noise
	 region defined by delta1. If given the noise region as an
	 argument, e.g. dsnf1max(400), the macro calculates the best
	 S/N ratio based on 400 Hz of noise region in the f1 dimension.
