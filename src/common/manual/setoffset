setoffset:     Calculates offset frequency for a given nucelus and ppm(M)

	Syntex:		setoffset(nucleus,ppm):offsetfreq
	Description:	Using the setref macro, setoffset calculates the
			offset frequency for a given chemical shift and
			returns the value
        
	Example:	setoffset(tn,5):tof
			setoffset('C13',85):dof       		
