"macro manualplot"
exists('plotoption','parameter'):$ex
if ($ex<0.5) then return endif

$size=size('plotoption')
$i=1
repeat
   if (plotoption[$i]<>'') then {plotoption[$i]}  endif
   $i=$i+1
until $i>$size
