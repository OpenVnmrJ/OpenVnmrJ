"macro donelocref"
    if (auto = 'y') then
	flush
        strstr(file,'.fid','last'):$ret,$file,$ext
        if ($ret=0) then $file=file endif
        $file=$file+'.fid'
        shell('rm -rf '+$file):$dum
    endif

