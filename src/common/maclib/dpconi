"macro dpconi"
"dpconi - macro for dconi('dpcon')"

if $# = 0 then
  dconi('dpcon')
endif
if $# = 1 then
  dconi('dpcon',$1)
endif
if $# = 2 then
  dconi('dpcon',$1,$2)
endif
if $# = 3 then
  dconi('dpcon',$1,$2,$3)
endif
if $# > 3 then
  write('line3','too many arguments for this macro')
endif


