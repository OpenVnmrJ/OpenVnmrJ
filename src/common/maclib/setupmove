"macro setupmove"
jexp:$exp
exists('move1','parameter'):$e
  if not $e then create('move1','integer') move1=$exp endif
exists('move2','parameter'):$e
  if not $e then create('move2','integer') move2=$exp endif
