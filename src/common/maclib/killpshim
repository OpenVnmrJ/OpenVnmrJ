"macro killpshim"
"macro killpshim"
" Abort proshim"

$cmd='touch '+systemdir+'/acqqueue/shimfitAbort'
shell($cmd):$dum
if (not $##) then
  write('line3','Aborting Proshim. It may take a few seconds.')
endif
