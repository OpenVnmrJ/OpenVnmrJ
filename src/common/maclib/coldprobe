"macro coldprobe"
" coldprobe turns on the cryo rof2 safety feature"

if (probestyle='cold') then
  write('line3','Probe configued as a cold probe')
else
  write('line3','Probe style not configued as a cold probe. Use tool to reconfigure')
  ProbePopup
endif
