"macro Autoproc3D"
if ($# > 0) then 
  $fidex=0 $3dfile=''
  AutoCheck($1,'fid'):$fidex,$3dfile
  if ($fidex) then
    rt($3dfile)
  else
    return
  endif
endif

  $banner='Processing '+seqfil
  banner($banner)
  ft3d('fg','xall')

