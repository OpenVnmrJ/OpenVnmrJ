"macro sethermes"
" sethermes - set tcl parameters for hermes       "
"             called by go, change, sample, and su"
if (traymax=768) then
  on('loc','global'):$e
  if ($e < 0.5 or loc < 0.5) then
     return
  endif
endif
