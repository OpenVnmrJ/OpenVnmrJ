"macro wtia"
"**********************************************************************"
" wtia :  used for interactive weighting display of 2D absorptive NMR  "
"         data  (FID's and interferograms)                             "
"**********************************************************************"

if ($# < 1) then
   wti('ptype')
else
   if ($# < 2) then
      wti('ptype',$1)
   else
      write('line3','wtia(<arg1>):  too many arguments')
   endif
endif
