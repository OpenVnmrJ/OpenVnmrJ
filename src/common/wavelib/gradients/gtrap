# gtrap: trapezoidal gradient
# the slopes are adjusted using the max parameter ( from 0 to 1)
# max=1 gives triangular shape
%
#       Shape Parameters
#       ----------------
amf 	= tra	 (amplitude modulation function)
steps 	= 200	 (default number of steps)
dash    = max     (dash parameter, set to dc-level)

#       Truncation Parameters
#       ---------------------
max     = 0.2

