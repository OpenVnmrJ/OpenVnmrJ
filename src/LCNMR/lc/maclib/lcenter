"macro lcenter"
/*-*- Mode: MAGICAL -*-*/
"macro lcenter"

" Set up the automation queue, if required "

setNewLcid      // Make sure we have a clean directory for the data.
if (lcexp='enter' or lcexp='enterA' or lcexp='stop') then
  walkupQ_lcnew
//  write('line3','Use the study queue to select experiments to run on each peak.')
//else
//  write('error','Sample entry is not necessary for this mode of operation')
endif
