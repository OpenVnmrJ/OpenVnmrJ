#!/bin/bash

# Install arm 64 packages

sudo apt update -y
sudo apt install -y  gcc-aarch64-linux-gnu g++-aarch64-linux-gnu

# To build the patch go to OpenVnmrJ/src/patch and run

# scons -f SConstruct_arm



