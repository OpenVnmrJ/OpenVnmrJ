"macro se3d"
"***********************************************************************"
" Set up parameters for:  						"
"  se3d - 3D spin echo imaging sequence.				"
"***********************************************************************"
rtppar($0,'nomenu'):$e
if ($e=0) then
  write('error','No parameters for pulse sequence %s',$0)
endif
