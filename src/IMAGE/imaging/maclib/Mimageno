"macro Mimageno"

" Mimageno - Select image number"
"            sets the parameter element"

" Usage: Mimageno(array_element)"

if ($# = 1) then
  element = $1
endif
if (element = 0) then 
  element = 1 
endif
