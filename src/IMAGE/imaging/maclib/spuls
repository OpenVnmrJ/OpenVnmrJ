"macro spuls"
"***********************************************************************"
" Initial set up experiment for imaging experiments:                    "
"  s2pul sequence is run. Parameters have been set up for imaging mode  "
"***********************************************************************"
rtppar($0,'nomenu'):$e
if ($e=0) then
  write('error','No parameters for pulse sequence %s',$0)
endif
