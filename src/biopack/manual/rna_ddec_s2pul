  Parameters:
             normal s2pul parameters, with rfchannel 4 status decoupling
    
Enter a value of dpwr3 appropriate for broadband decoupling (e.g. 36). Enter
a dmf3 value equal to 1e6/pw90 at this dpwr3 (use ddec_pwxcal experiment to
determine pw90(2H) at dpwr3. Enter the desired modulation mode, e.g. dmm3='cgc'
or 'cwc'. Enter dm='nyn' to enable 2H decoupling. dof3 should be set at
about 3ppm in the 2H spectrum.

This sequence can be used to verify the best value of dof3. Vary dof3 over   
+-2000 Hz with proper dpwr3 and dmf. The dof3 value in the center of the
decoupled multiplets should be at 7.15ppm (C6D6). The value for RnaPack
sequences should be 5ppm to cover all aliphatic deuterons, hence            
dof3=dof3-2*dfrq3. Use this new dof3 value in the RnaPack parameter sets,
along with dpwr3 and dmf3. Enter these values when using the Install process,
or when using "rnapack2".
