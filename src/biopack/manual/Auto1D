Use Probefile Values
********************

This button will start an Automatic 1D  process in which the current parameter 
values in the Probefile are used as starting points.

Use this tpwr/pw
***************
This button will start an Automatic 1D process in which the current parameter 
values in the Probefile are used as starting points except for tpwr and pw, for which 
the tpwr and pw values in the current parameter set are used.

Calibrate 1H pw90 First
***********************
This button will start an Automatic 1D  process in which the current parameter 
values in the Probefile are used as starting points except for pw (1H pw90)
for which an autocalibration is done. The Probefile is updated after the new
value is found.

