

probe=''
fread(systemdir+'/user_templates/global','global','reset')
write('line3','after system fread')
probe?

fread(userdir+'/globaltest','global','value')
write('line3','after globaltest fread')
probe?
display('probe','global')

