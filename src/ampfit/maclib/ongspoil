"macro ongspoil"
"macro ongspoil"
"********************************************************************
"    Interface macro to determine if gspoil parameter should be
"    enabled
"********************************************************************
on('gspoil'):$e
if ($e > 0.5) then
  if (adjgspoil='y') then
    return(1)
  else
    return(-1)
  endif
endif
return(-1)


